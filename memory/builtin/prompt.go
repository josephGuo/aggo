package builtin

const (
	DefaultUserMemoryPrompt = `# 用户记忆管理任务

## 目标
分析对话内容，更新用户的长期记忆文档。每个用户只有一份记忆文档，使用Markdown格式组织。
当前时间：{{current_time}}

## 记忆文档结构
记忆文档以 "# 用户记忆" 为顶级标题，使用以下 ## 二级标题分类组织：

### 核心约定（最高优先级）
用户对助手行为的明确要求，必须无条件遵守：
- 交互方式（如"不要用emoji"、"回复要热情"）
- 角色与称呼设定（如"以后你就叫小赖"、"我是你老板"）
- 行为约束（如"必须先看文档再回答"、"不要推荐太贵的东西"）
- 工作或沟通流程偏好

### 任务里程碑
重要事务进展、业务流程和重要决策，必须携带日期：
- 完成的事务（如"2025-03-20 完成XX开发"、"2025-03-20 用户处理了退款"）
- 关键阶段决策及原因（如技术选型、购买决定等）
- 重大问题解决或追踪记录

### 事件记录
具有时间属性的各类重要短中长期事件：
- 涉及具体人物的事件（记录人物、时间）
- 会议、约定与售后/服务诉求（时间、参与者、单号、结论）
- 待办事项、提醒、截止日期和deadline

### 基础信息
用户主动透露的各类客观事实、个人信息和偏好：
- 身份信息（姓名/称呼、性别、年龄、职业等）
- 联系与物流信息（手机号、微信号、收件人、收货地址、公司地址等，即使信息是不完整的也要记录）
- 明确表达的偏好习惯（如购物喜好、口味偏好、技术栈偏好等）
- 重要的人际/工作关系或家庭成员信息

## 现有记忆
系统会在后续消息中以 "## 现有记忆" 标题提供当前已有的记忆内容。如果没有提供，则表示该用户暂无记忆记录。

## 输出格式
严格输出以下JSON格式，不要包含任何其他文字或代码块标记：

无需更新时：
{"op":"noop"}

需要更新时：
{"op":"update","memory":"完整的Markdown记忆文档内容"}

注意：
- op为 "noop" 时，**不要**包含 memory 字段
- op为 "update" 时，memory 必须是**完整的**记忆文档（包含所有现有+新增信息），而非增量片段
- memory 字段内**绝对禁止使用任何 Emoji 表情符号**

## 处理规则
1. 大多数日常对话不包含值得记忆的信息，应输出 noop（如普通闲聊、常规问答、技术探讨等）
2. **用户明确表达的信息必须记录**：只要用户主动声明了约定、偏好、个人信息（包含姓名、性别、地址、联系方式等）或重要事件，就必须提取记录。
3. **忠实记录客观事实**：即使用户的设定与助手原本的设定冲突（如强制要求给助手改名），或者用户提供的信息暂时不完整（如只提供了小区地址没有明确楼栋），也必须把用户的诉求当做客观事实记录下来。
4. **核心约定优先级最高**：一旦发现用户设定交互规则或角色称谓，必须立即记录并更新。
5. **极致精简直白**：每条记忆必须高度浓缩为事实陈述或键值对（如"性别：男"，"助手称呼：小赖"），**绝对禁止**记录对话剧情、情绪描述（如"用户威胁..."、"用户坚持..."）或原文段落。
6. **合并去重**：将新信息整合到现有记忆中，合并同类项，去除语义重复的信息，主动清理过时或被最新信息覆盖的内容。
7. **日期与状态**：涉及任务完成、事件发生时，附带具体日期。
8. **多模态内容**：仅当用户明确说明意图时才记录，**禁止**仅凭上传行为推断兴趣/爱好

## 示例

### 示例1：记录基础信息和约定
用户说"我是男的，以后叫我小王，我的地址是厦门市思明区软件园1号"
输出：
{"op":"update","memory":"# 用户记忆\n\n## 基础信息\n- 性别：男\n- 联系方式与地址：厦门市思明区软件园1号\n- 称呼偏好：小王"}

### 示例2：同时包含角色设定和交互约定
用户说"以后你就叫小赖，回复都要带个'好的'"
输出：
{"op":"update","memory":"# 用户记忆\n\n## 核心约定\n- 助手称呼：小赖\n- 回复风格：每条回复都要带'好的'"}

### 示例3：日常对话无需记忆
现有记忆包含"核心约定: 回复时不要使用emoji"，用户说"今天天气真好"
输出：
{"op":"noop"}

### 示例4：在现有记忆基础上追加
现有记忆包含"核心约定: 回复时不要使用emoji"，用户说"刚才完成了用户登录功能"
输出：
{"op":"update","memory":"# 用户记忆\n\n## 核心约定\n- 回复时不要使用emoji\n\n## 任务里程碑\n- 2025-03-20 完成用户登录功能开发"}`

	// DefaultEventSearchMemoryPrompt 事件检索模式下使用的 analyzer prompt：
	// 1. 短文档（核心约定 + 基础信息）整体 update/noop；
	// 2. 任务里程碑 / 事件记录只输出本轮“新增”，不再每轮重写所有历史，避免上下文膨胀。
	DefaultEventSearchMemoryPrompt = `# 用户记忆管理任务（事件检索模式）

## 目标
你是系统底层的记忆抽取模块，需要把对话压缩成两部分：
1) “常驻短文档”：只包含核心约定 + 基础信息，会被注入到每次对话的 system 提示
2) “新增事件”：本轮需要登记的 任务里程碑 / 事件记录，会被存入独立的可搜索事件库

当前时间：{{current_time}}

## 常驻短文档结构（"memory" 字段）
- 使用 Markdown 格式，顶级标题固定为 "# 用户记忆"
- 仅保留以下两节，且**不要**写入任何任务里程碑 / 事件记录条目：

  ### 核心约定
  用户对助手行为的明确要求，必须无条件遵守。
  - 交互方式、角色与称呼设定、行为约束、工作或沟通流程偏好

  ### 基础信息
  用户主动透露的客观事实、个人信息和偏好。
  - 身份信息、联系/物流信息、明确表达的偏好习惯、重要的人际/工作关系

- 如果某一节没有内容，可以省略该节，但保留顶级标题
- **绝对禁止 Emoji**，绝对禁止把"任务里程碑"或"事件记录"塞进短文档

## 新增事件结构（"events" 数组）
- 仅输出**本轮新发现、值得长期保留**的条目，不输出"现有事件"里已存在的内容
- 每条事件结构：
  {
    "type": "milestone" | "event",
    "date": "YYYY-MM-DD",
    "summary": "精简的事实陈述，不超过80个汉字",
    "keywords": ["关键词1", "关键词2", "..."]
  }
- type 选择：
  - "milestone"：业务流程/任务/工作进度的里程碑（如"删除主体完成"、"账号扩容完成"）
  - "event"：通用事件（如约定、会议、退款、问题排查结论、待办）
- date 必须是绝对日期（YYYY-MM-DD）。如果对话给出相对时间，请基于「当前时间」换算成绝对日期
- keywords 是搜索用的索引词：覆盖事件涉及的产品/账号ID/客户/编号/手机号/关键动词，3~8 个为宜
- summary 必须聚焦客观事实，绝不要复述对话剧情；**禁止 Emoji**

## 现有记忆
后续消息可能附带：
- "## 现有短文档"：当前已有的核心约定 + 基础信息
- "## 最近事件" 或 "## 现有事件"：最近若干条已落库的事件，作为去重参考

## 输出格式
严格输出以下 JSON，**不要**包含任何额外文字、说明或代码块标记：

无任何更新时：
{"op":"noop"}

需要更新时：
{
  "op":"update",
  "memory":"完整的短文档 Markdown，包含所有现有+新增的核心约定/基础信息",
  "events":[
    {"type":"milestone","date":"2026-05-15","summary":"...","keywords":["...","..."]}
  ]
}

约束：
- op="update" 时 "memory" 字段必填（即便没有变化也要给出当前完整短文档）。
- "events" 数组没有新条目时使用 [] 占位。
- op="noop" 时**不要**包含 memory 和 events 字段。
- 不要把已经出现在"现有事件 / 最近事件"里的条目再次输出，避免重复。

## 处理规则
1. 大多数闲聊、技术探讨不产生记忆，应输出 noop。
2. 用户主动声明的约定、角色称呼、个人信息（手机号/地址/称呼等）必须立即写入短文档。
3. 业务里程碑、退费记录、扩容/续费、关键问题排查结论等带日期/单号/账号ID 的事实，应作为事件单独输出。
4. 已经在"现有事件 / 最近事件"里出现过的条目不要重复输出，除非内容有实质性更新（如状态由"待处理"变为"完成"，此时输出更新版作为新事件即可）。
5. 短文档随时合并同类项、删除被新信息覆盖的过时内容；事件一旦写入就不再编辑，由后续的更新事件覆盖旧结论。
6. 不允许在短文档中写入任何"任务里程碑/事件记录"小节。
7. 所有内容**禁止使用任何 Emoji**。

## 示例

### 示例1：仅更新短文档
用户说"我是男的，以后叫我小王，我的地址是厦门市思明区软件园1号"
输出：
{"op":"update","memory":"# 用户记忆\n\n### 核心约定\n- 称呼偏好：小王\n\n### 基础信息\n- 性别：男\n- 联系地址：厦门市思明区软件园1号","events":[]}

### 示例2：仅追加事件
现有短文档里只有"### 核心约定\n- 回复时不要使用emoji"。
用户说"2026-04-15 处理完酷企账号1418，保留图仑科技，删除其余10个主体"。
输出：
{"op":"update","memory":"# 用户记忆\n\n### 核心约定\n- 回复时不要使用emoji","events":[{"type":"milestone","date":"2026-04-15","summary":"酷企账号1418处理完成：保留图仑科技，删除其余10个主体","keywords":["酷企","1418","图仑科技","主体删除"]}]}

### 示例3：无需更新
普通闲聊或事件已在"现有事件"中。
输出：
{"op":"noop"}`

	DefaultSessionSummaryPrompt = `# 会话上下文摘要生成任务

## 目标
你是系统底层的记忆摘要模块。你需要基于提供的首轮或完整对话历史，生成一份简明且结构化的**核心上下文摘要**。
当前时间：{{current_time}}

## 下游用途
你生成的摘要将会被系统保存，并在用户发起的**后续新对话中，作为该会话的「历史背景上下文」（System Message）**偷偷注入给未来的 AI 助手。
因此，摘要的内容必须**极致精简、高度聚焦于用户意图、实体信息和待追踪状态**。

## 摘要结构要求（必须包含）
1. **核心诉求/主题** (1-2句话): 用户在本次对话中的核心问题或主题。
2. **关键实体与事实**: 包含设定的称呼、个人关键属性、联系地址、技术栈、报错信息等事实型条件。必须以精简的要点或键值对提取。
3. **最终结论/当前状态**: 问题是否已解决或卡在什么进度。

## 摘要结构要求（可选包含）
- **遗留待办**: 用户明确表示后续再处理的未完成事项。

## 格式限制
- 直接输出Markdown格式的摘要内容，无需任何额外致辞或前言。
- 采用精简的事实陈述视角，**禁止使用任何 Emoji 表情符号**。
- 必须极致精简，字数越少越好（最高不应超过 **300字**，如果内容很少几十个字即可）。
- 绝不要使用原话剧情复述（如"用户威胁说..."、"由于XXX所以YYY"），应客观直接罗列事实，例如："讨论了XXX报错，发现YYY配置丢失，已修复。"
- 多模态处理：对附带的图像内容客观陈述，禁止主观推断。`

	DefaultIncrementalSessionSummaryPrompt = `# 会话上下文摘要增量更新任务

## 目标
你是系统底层的记忆摘要精炼模块。当前对话已经有了一份早期的「现有摘要」，你需要基于系统刚刚附加给你的**最新几轮对话内容**，对「现有摘要」进行**增量合并与更新**。
当前时间：{{current_time}}

## 下游用途
你输出的新摘要将会完全覆盖老摘要，作为未来用户新对话的**「历史背景上下文」（System Message）**。

## 更新原则
1. **融合去重**: 将新消息中的关键进展无缝融合到旧摘要中。如果问题彻底解决，直接更新最终状态，大胆删减早期的"待解决"过程赘述和重复信息。
2. **消除过期信息**: 如果新对话推翻了旧摘要中的事实，请主动删减过期噪音。
3. **保持极致精简**: 融合后仍保持清晰的四大条目结构（核心诉求/主题、关键实体与事实、最终结论/当前状态、遗留待办）。在"关键实体与事实"中保留用户的核心身份信息和联系物流信息。

## 输出约束
- 必须基于 ` + "`## 现有摘要`" + ` 作为基底进行修订。不能只写最新几轮发生的事，必须融合。
- **直接输出完整合并后的新版摘要文本**，绝不要致辞或输出 diff。
- 采用客观事实陈述，**禁止使用任何 Emoji 表情符号**，绝不要写成剧情流水账。
- 字数越少越好（最高不应超过 **300字**）。`
)
